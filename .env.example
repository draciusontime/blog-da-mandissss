# MongoDB Connection URI
# Para MongoDB Atlas (nuvem), substitua pelos seus dados reais
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/blog-database?retryWrites=true&w=majority

# Para MongoDB local (desenvolvimento)
# MONGODB_URI=mongodb://localhost:27017/blog-database

# Session Secret (use um valor único e seguro)
SESSION_SECRET=segredo-super-seguro-mude-isso

# Port (Railway usa automaticamente process.env.PORT)
PORT=3000

# Admin User (configure suas credenciais)
ADMIN_USERNAME=mandis
ADMIN_PASSWORD=020407